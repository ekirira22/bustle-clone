{{#if addNewPost}} <!--Now get this, the addNewRental is the value that is either true or false, hence the if statements below it -->
  <h1>New Blog </h1>
  <div>
    <form class="jumbotron">
      <div class="form-group">
        <label for="owner">Name for Author</label>
        {{input value = owner id="owner"}}
      </div>
      <div class="form-group">
        <label for="head">Put a Heading</label>
        {{input value = head id="head"}}
      </div>

      <div class="form-group">
        <label for="snippet">Short Description of the Blog</label>
        {{input value = snippet id="snippet"}}
      </div>

      <div class="form-group">
        <label for="body">Post content</label>
        {{input value = body id="body"}}
      </div>

      <div class="form-group">
        <label for="image">Image URL</label>
        {{input value = image id="image"}}
      </div>
      <button {{action 'savePost1'}} >Save </button>
      <button {{action 'hideForm'}}> Hide</button> <!--We've added this action that hides the form when clicked-->

    </form>
  </div>
{{else}}
  <button {{action 'rentalPostShow'}}> Add a new Rental</button>
{{/if}}
