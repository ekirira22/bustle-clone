<h1>Bustle Clone Ideas</h1>

{{#each model as |post|}}
  {{post.owner}} who is an author has written a book {{post.head}}, <br>
  {{post-tile post=post }}
  <h2>{{post.snippet}}</h2> <br>
  <h4>{{post.body}}</h4> <br>
  <p>
    <img src={{post.image}} alt=":(" />
  </p>
  {{new-post savePost2=savePost3}}
{{/each}}
